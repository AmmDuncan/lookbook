# Evidence — Search & Results (P-S calibration)

Method: studied 12 real web search/results surfaces across five contexts (in-app record search, command-palette global search, marketplace/catalog browse, search-engine-style results, and the no-results empty), read by **(a) craft to learn** and **(b) how the rules are applied** — where reality confirms a rule, calibrates a number, or breaks a rule (→ rule is wrong/too strict/needs scope). The strongest calibrations are the ones the no-results screens forced: this surface is judged on its empty case, and the screens disagree most there.

Screens: Databricks (Search results "simple" — right category rail + Type/Owner/Location chips), Amplitude (command-palette modal + right-rail facets + "3,639 results found in 14ms"; and a results page w/ right-rail facets + Relevance sort), Jira (search dropdown w/ typeahead + facets-in-dropdown), Mixpanel (search results + right-rail type facets + Relevance sort), Gemini (results list), Faire (3 marketplace screens — left filter rail w/ search-within-facet, top filter pills, "2 filters applied · Clear all", "22,184 participating brands", Sort by Featured, media-card grid), Relevance AI (marketplace "Results for design" — top filter dropdowns + media cards), Firecrawl (web/news results w/ "only show 10 results" honesty), Dovetail (no-results + illustration + "adjust your search filters"), a screen-library tool (no-results "Try these popular searches" + query chips), Twist (no-results "No matches for '' · Try filtering"), Digg (no-results "Hmm, your search didn't turn up anything" — dead end), 7shifts (search form: Location/Category/Posted-by/Date-range + tabbed results). 2026-05-29.

## Rule-application reading

| Rule | What real search pages do | Verdict |
|---|---|---|
| Search input (P-S-01) | Faire "Search products or brands" full-width + magnifier + ✕; Amplitude centered modal field; Databricks/Jira top-bar field; all ≥44px | **CALIBRATE → P-S-01**: input is the anchor, scope-naming placeholder, leading magnifier + trailing clear. Bare "Search" placeholder is the F70 tell. |
| Typeahead + facets-in-dropdown (P-S-02) | **Jira**: search dropdown shows work-item matches **+ "Filter by project/assignee/reporter/status" inside the dropdown** + recently-viewed | **NEW → P-S-02**: the dropdown can carry quick facets and recents — the user refines before a results page even loads. The fastest path; full page is the fallback. |
| Count + query echo (P-S-03) | Databricks "Search results for 'simple'"; Amplitude **"3,639 results found in 14ms"**; Faire "22,184 participating brands" | **CALIBRATE → P-S-03 (refines F63/F72)**: count + echoed query is the first thing the eye needs — the system confirming it understood. Timing aside (14ms) is optional polish. A list with *neither* is the #1 search tell. |
| Active-filter chips (P-S-04) | **Faire "2 filters applied · Clear all"** + value pills ("Low minimum", "American brands"); Databricks Type/Owner/Location chips | **NEW → P-S-04**: active facets must surface as removable chips + a "Clear all" at ≥2 — otherwise narrowing is invisible state and over-narrowing reads as an empty catalog (feeds P-S-09 recovery). |
| Sort control (P-S-05) | Faire "Sort by Featured" top-right; Amplitude & Mixpanel **"Relevance ▾"** top-right of the list | **CALIBRATE → P-S-05 (refines F16)**: one sort select, top-right, **Relevance default for a query**. Alphabetical-default on a query ignores the match score → forbidden. |
| Facet placement (P-S-06) | **Left rail**: Faire (Holidays/Brand values/Location + search-within-facet + Show more). **Right rail**: Amplitude (Type/Content/Last edited/Space/Editor/Project), Databricks (category list), Jira-dropdown, Mixpanel. **Top bar**: Databricks chips, 7shifts (4 dropdowns), Faire top pills | **CALIBRATE → P-S-06 (contextual — the dynamic branch)**: placement tracks how central filtering is + facet count. Left = "filtering is the job" (marketplace); right = "narrow if you want" (app over a query); top = few facets, wide results. **One-or-two-facet rail is the failure mode.** |
| Result-item shape (P-S-07) | **List row**: Amplitude/Databricks/Gemini/Jira (icon + title + meta). **Media card grid (3–5up)**: Faire (image + name + rating + min + ♥/+Add), Relevance AI (icon + title + body + author + Free). **Rich row**: Firecrawl (title + snippet + url) | **CALIBRATE → P-S-07 (contextual on content type)**: row when the title carries the match, card when the image is the decision, rich row when a snippet aids the click. Item shape follows what the user decides on. |
| Pagination vs infinite (P-S-08) | Faire **infinite media grid** (browse); Firecrawl **"This playground can only show 10 results"** (honest cap); record lists short enough to not page | **NEW → P-S-08 (contextual)**: infinite for browse-and-discover, explicit page/load-more for find-a-record (position + back matter). Truncation is fine *only if disclosed* (Firecrawl). |
| First-run vs no-results (P-S-09/10) | **First-run**: Jira + a screen-library tool show recents + popular-search chips on focus-empty. **No-results**: Dovetail "adjust your search filters", "Try these popular searches" + chips, Twist "Try filtering to refine", **Digg dead-end** | **CALIBRATE → P-S-09/10**: the two states are *different messages*. First-run = "start here" (no query echo, warm). No-results = "your query 'X' matched nothing, recover thus" (echoes query, actionable). Conflating them strands the user. |
| No-results recovery (P-S-09) | Dovetail (broaden/adjust filters + Learn more), suggested-query chips (**strongest** recovery), Twist (try filtering), **Digg ("Hmm…") = no action at all** | **CALIBRATE → P-S-09 (refines F69)**: recovery = broaden / clear-filters / suggest. **Digg is the cautionary case** → fed the forbidden "dead-end no-results." Over-narrowing is the most common zero cause → a one-tap "Clear all filters" is the highest-value recovery. |
| Loading skeleton (P-S-09) | (not captured live) — but every list/grid has a fixed item shape to mirror | **APPLY → P-S-09**: skeleton mirrors the row/card shape, never a centered spinner (the page must not reflow on arrival). |
| Error vs no-results (P-S-09) | Firecrawl shows a "Report issue" affordance; the distinction is logical not always captured | **APPLY → P-S-09 (refines F33/F69)**: a *failed* search needs retry copy, never no-results copy — else the user edits a query that was never run. |
| Matched-term visibility (P-S-11) | Databricks/Gemini show the query term in titles; faceted browse (Faire) uses the active-filter chips as the "why these" signal | **NEW → P-S-11**: highlight the matched substring; for facet-only browse the chips play that role. Zero visible connection to the query = reads as an unfiltered dump. |
| F26/F27 density | Result rows compact (36–48px), facet rail + input medium; Faire grid generous | **CONFIRM split band**: list is compact (scanned like a table), header/rail medium. |
| F36 texture | **zero texture on populated lists**; Dovetail/Twist/Digg empties carry an illustration/3D mark | **CONFIRM → P-S-09**: the no-results zone is the *one* place texture earns its keep (empty = sparse surface); the list stays clean. |
| F50 mobile | (desktop captures) rails are filter state that must become a drawer, not vanish | **APPLY → P-S-06**: rail reflows into a "Filters (n)" drawer; active-filter chips stay above the list. |

## The dynamic-generation lesson
Search surfaces vary on **two axes at once**: *how central filtering is* (drives facet placement — left rail when filtering IS the job/marketplace, right rail when it's optional refinement over a query, top bar when there are ≤5 facets) and *what the user decides on* (drives item shape — a title → list row, an image → media card, a snippet → rich row). A marketplace (Faire) is left-rail + media-grid + infinite scroll; an in-app record search (Amplitude/Databricks) is right-rail + list-rows + relevance-sort; a global command palette (Amplitude modal, Jira dropdown) collapses the whole thing into a focused overlay with facets-in-dropdown. The constant is the *reading order* — input → count+echo → active-filter chips → sort → results → (and always) the four states; the **facet placement and item shape flex to the content and the centrality of filtering.** Stamping a left rail onto a two-facet query search, or a media grid onto a title-only record search, is the proof it was templated, not reasoned.

## Over-strict / wrong-rule catches (the strongest calibrations)
- **Digg's "Hmm, your search didn't turn up anything"** echoed the query but offered *no recovery* — proved that "ship a no-results state" (F33) isn't enough; the state must be *actionable* (broaden / clear-filters / suggest), not just present. Fed the forbidden "dead-end no-results" and sharpened P-S-09 against F69. (Mirrors the detail study's HubSpot-`(0)` catch: an empty state is a *designed, actioned* state, not a polite blank.)
- **Comparing the no-results screens (Twist, Dovetail, the suggested-query empties)** showed the no-results state and the first-run state are *different messages* that lazy designs collapse into one — first-run has no query echo and is suggestive; no-results quotes the query and is corrective. Fed P-S-10 (never reuse one for the other) — a tell invisible to every rule check because each state looks fine alone.
- **Faire's "2 filters applied · Clear all"** proved active filters need *removable chips + a clear-all*, not just a rail of checked boxes — because the most common cause of a near-empty result list is over-narrowing, and the user's recovery is "clear filters," which has to be one tap from the chip row (P-S-04 → P-S-09).
- **Jira's facets-inside-the-search-dropdown** broke the naive "facets live in a rail on the results page" assumption — the fastest refinement happens *in the typeahead dropdown*, before a results page loads at all (P-S-02). The results page is the fallback, not the only home for facets.

## Calibrations applied to patterns/search.md
1. Search input → **anchor, scope-naming placeholder, magnifier + clear, ≥44px** (P-S-01).
2. Typeahead → **recents/suggested + quick facets in the dropdown** (P-S-02).
3. Header → **count + echoed query first**, timing aside optional (P-S-03).
4. Active filters → **removable chips + "Clear all" at ≥2** (P-S-04).
5. Sort → **one select, top-right, Relevance default for a query** (P-S-05).
6. Facets → **contextual placement** (left=marketplace / right=app-over-query / top=≤5 / drawer=mobile) — the dynamic branch (P-S-06).
7. Result item → **contextual shape** (row / media card / rich row) by what the user decides on (P-S-07).
8. Paging → **infinite for browse, explicit for find-a-record; keep count+filters visible** (P-S-08).
9. States → **first-run ≠ loading ≠ no-results ≠ error**, each its own message; no-results echoes the query + a recovery path (P-S-09/10).
10. Matched term → **highlight it; chips carry it for facet-only browse** (P-S-11).

---

## Run-2 — live-UI calibration pass (2026-05-29)

Method: a fresh reference study of **~36 real, public, shipped search surfaces** across the five contexts — command-palette / global search (Navattic, Vapi, Whop, GitHub, Fey, Replit, Clay, Linear, Gamma), faceted marketplace results (Selfridges, Nike, Walmart, Klarna, Amazon, Farfetch, adidas), no-results empties (Render, Dovetail ×2, Unity, adidas, Vercel, OpenSea, Midday), and typeahead dropdowns (Amazon, Codecademy, Shop, Skillshare, Udemy, Tines, Grok, Pinterest). Plus two design references on command-palette construction (real, public writeups). Read rule-by-rule for VALIDATE / CALIBRATE / TOO-STRICT. Discipline: **refine-don't-add** — the strongest output is a number grounded or a rule contextualized, not a new rule. This pass yielded **number/branch calibrations only; zero new rules adopted** (two NEW-RULE CANDIDATEs flagged for battle-test, not added).

### Rule-application reading

| Rule | What this run's screens showed | Verdict |
|---|---|---|
| P-S-01 (input anchor) | Faire-class scope placeholders everywhere: Walmart "Search everything at Walmart online and in store", OpenSea "Search items, collections, and accounts", Whop "Search across Whop". Bare "Search" only on chrome-collapsed bars (Farfetch, adidas top-bar). Magnifier-leading universal; ✕-clear when non-empty (Amazon, Pinterest, Codecademy, adidas). | **VALIDATE** — scope-naming placeholder + magnifier + clear holds across 36 screens. No change. |
| P-S-01 / palette width | Command-palette modals cluster tight: Linear/Clay/Fey/Vapi all **~520–560px** wide; Gamma ~500px (carries thumbnails); GitHub/Replit inline-anchored but similar body width. Centered, backdrop-dimmed, vertically high (top third). | **CALIBRATE → P-S-01**: add the palette-modal width number (**~480–560px, ~520 typical**) — currently P-S-01 says "command-palette modal" with no dimension. Grounds the composition default. |
| P-S-02 (typeahead + facets/recents in dropdown) | **Grouped typeahead** universal: Skillshare "CLASSES / TEACHERS", Codecademy queries-then-"Top results", Udemy queries-then-courses, Pinterest queries-then-people-then "Search your Pins". **Recents on focus-empty**: Tines "Recent" + tag filter, Grok "today" list, Shop full "Search history" panel, Vapi/Replit/Clay grouped actions+recent. Pinterest/Vapi carry a scope-switch affordance in the dropdown footer. | **VALIDATE (strengthened)** — grouping-by-type and recents-on-focus-empty confirmed at scale; the "quick facets in the dropdown" sub-claim is the *rarer* form (Jira-style) but the dropdown-as-fastest-path core is universal. No change to wording; corroboration added. |
| P-S-03 (count + echo) | Selfridges "Search results: 'dress' · 3,369 results", Nike "socks (126)", Walmart "Results for 'Socks' (10)", Klarna "51 products", Amazon implicit count. Walmart adds source aside "Uses external data. Price when purchased online ⓘ" (the optional-polish freshness note). | **VALIDATE** — count + echoed query is the first header element across every populated results screen. Source/freshness aside confirmed as optional polish. No change. |
| P-S-04 (active-filter chips + Clear all) | Klarna "Up to $50 ✕ · At least 50% ✕ · Clear filters"; adidas drawer "Clear All". Marketplace rails also expose per-group "Clear" (Walmart Brand "Clear", Fulfillment "Clear"). | **VALIDATE** — removable chips + clear-all confirmed; note the *per-group* clear as a complementary form alongside the global chip-row clear. No change. |
| P-S-05 (one sort select, top-right, Relevance default) | Selfridges "Sort ↕" top-right, Nike "Sort By ▾", Klarna "Sort by popularity ▾", OpenSea "Recently received ▾", Midday "Sort by activity ▾". Browse defaults to Featured/Popularity (no text query); text-query screens default to relevance. | **VALIDATE** — single select, top-right, browse-default = Featured/Popularity, query-default = Relevance. No change. |
| P-S-06 (contextual facet placement) | **Left rail dominant for marketplace**: Selfridges, Nike, Walmart, Klarna, Amazon, Farfetch all left. **Right drawer even on desktop**: adidas "Filter & Sort" sheet with "APPLY (187)". **"Hide Filters" toggle** (Nike) collapses the rail to widen the grid. **Collapsible accordion facet groups** (Selfridges, Nike, Farfetch, Klarna chevron each group). | **VALIDATE + CALIBRATE → P-S-06**: left-for-marketplace / drawer-for-narrow all confirmed. Add two real details the rule omits: facet groups are **collapsible accordions** (chevron per group) and the rail often has a **"Hide filters" toggle** to reclaim grid width. adidas proves the drawer form appears on desktop too (not mobile-only). |
| P-S-07 (contextual item shape) | **Media card grid** for all marketplace (Selfridges/Nike/Walmart/Klarna/Amazon/Farfetch/adidas: image + name + price + facts like colors/rating/discount + ♥ save). **List row** for in-app/palette (Vapi/Linear/Clay/GitHub icon+title+meta+shortcut). **Rich row** (Amazon: thumb + title + rating + price + delivery snippet). | **VALIDATE** — three shapes confirmed by content type. Marketplace card facts cluster at price + 1–2 (colors, rating, discount badge). No change. |
| P-S-08 (paginate vs infinite + keep count/filters) | Selfridges "60 / 1,277 results · Show more results" (explicit load-more *with running count* — the textbook honest form). Marketplace grids otherwise infinite-scroll. | **VALIDATE (strengthened)** — Selfridges "60 / 1,277 · Show more" is the exemplar of "keep the count visible as the list grows." No change. |
| P-S-09 / P-S-10 (state set; no-results echo + recovery; first-run ≠ no-results) | **No-results**: Render "No matching results · Try adjusting your search and checking for typos · Reset search"; Dovetail "No results found. Try adjusting your search filters to include more results · Learn more" (+ illustration); Unity "No results found. Perhaps start a new topic? Or try searching with Google"; adidas "OOPS – NO RESULTS FOR 'ZXCVB'. Don't give up! Check the spelling, or try something less specific" (+ keeps the input + footer nav); OpenSea "No items found for this search · Back to all items"; Midday "No results · Try another search, or adjusting the filters · Clear filters"; Vercel "No Results Found · …did not return any results · New Project →". **First-run** distinct: Whop "Find anything · Start typing or use commands with '/'", Fey "Search commands", Clay grouped general-actions, Tines/Grok/Shop recents. | **VALIDATE (strongly)** — every no-results screen echoes + recovers (broaden / fix-spelling / clear-filters / suggest); first-run never echoes and is suggestive. The clear-filters/reset recovery (Render/OpenSea/Midday) confirms over-narrowing as the top zero cause. adidas echoing the *uppercased query* + keeping the input is best-in-class. No change; the split holds. |
| P-S-09 texture-in-empty (F36) | Dovetail no-results carries a single illustration; Unity a magnifier mark; populated grids carry zero texture. | **VALIDATE → F36 carve-out** — empty/no-results is the one search zone that earns one illustrative device. No change. |
| P-S-11 (matched-term visible) | **Codecademy** bolds + yellow-highlights the matched substring in *every* typeahead suggestion (textbook). Skillshare/Udemy/Pinterest bold the matched term in suggestions. Marketplace facet-browse uses the active-filter chips as the "why these" signal. | **VALIDATE (strengthened)** — highlight happens in the *dropdown* too, not only results; Codecademy is the exemplar. No change. |
| Command-palette keyboard-hint footer | **Near-universal in palettes**: Vapi "↑↓ to navigate · ↵ to select · 14 results", GitHub "⌘K E", Replit "⌘ K", plus per-row shortcut hints right-aligned (Clay N/P//, Linear A/I/S/P/L, Vapi ⌘+n). Design references corroborate the input→results→footer-hints structure. | **NEW-RULE CANDIDATE (flagged, NOT added)** — "command palette shows a persistent keyboard-hint footer (↑↓ navigate · ↵ select · esc close) and right-aligned per-row shortcut hints." Strong real-world support, but it's arguably a *composition default* under P-S-02 rather than a standalone rule. Battle-test before adopting; refine-don't-add says fold into P-S-02's composition note first. |
| Search/command mode duality ("/" or ">") | Whop "use commands with '/'", GitHub ">edit" switches to command mode, Gamma/Clay mix actions+navigation in one palette. | **NEW-RULE CANDIDATE (flagged, NOT added)** — "a global palette can carry both *find* (records) and *do* (commands), mode-switched by a '/' or '>' sigil." Real and consistent, but command-execution is arguably its own surface (actions, not search-results). Park for a possible `patterns/command-palette` split rather than bloating P-S. |
| F26/F27 density | Marketplace result cards generous (image-led); in-app list rows + palette rows compact (~36–44px); facet rail + input medium. | **VALIDATE** — the compact-list / medium-header split holds. No change. |

### Over-strict / wrong-rule catches
- **None this run.** No P-S rule was broken by an excellent screen without cost; every rule was either validated or had a number/branch *added* (palette width, collapsible-accordion facets, hide-filters toggle). The pass is a clean refine-don't-add: the brain's search surface has largely converged against current reality.

### NEW-RULE CANDIDATES (flagged for battle-test, NOT added — refine-don't-add)
1. **Command-palette keyboard-hint footer + per-row shortcut hints.** Near-universal in real palettes; corroborated by two design writeups. Likely belongs as a P-S-02 composition note, not a new rule. Battle-test the footer's design value before any adoption.
2. **Search/command mode duality (find vs do, "/" or ">" sigil).** Consistent across Whop/GitHub/Gamma/Clay. May warrant a future `patterns/command-palette.md` split rather than expanding P-S (command execution is a different surface from result-finding).

### Calibrations proposed to patterns/search.md (number/branch only)
1. **P-S-01** — add the command-palette modal width: **~480–560px (≈520 typical), centered, backdrop-dimmed, anchored in the top third.** Grounds the existing "command-palette modal" mention with the observed cluster (Linear/Clay/Fey/Vapi).
2. **P-S-06** — add two real facet-rail details: facet groups are **collapsible accordions (chevron per group)**, and the rail commonly offers a **"Hide filters" toggle** to widen the result grid; note adidas proves the **drawer form appears on desktop**, not only mobile.

Everything else **VALIDATED with no change** — P-S-02/03/04/05/07/08/09/10/11 and the F26/F27/F36 carve-outs were already grounded; this run strengthened their corroboration rather than moving any number.
