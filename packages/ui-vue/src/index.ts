/**
 * @lookbook/ui-vue — Vue 3 primitives implementing the Lookbook spec.
 *
 * STATUS: stub. Primitives are populated by the DVLA bridge pass, which maps
 * the existing `app/components/ui/` layer to the Lookbook gallery spec, aligns
 * them, and extracts them here. Each primitive must:
 *   - reference @lookbook/tokens (never raw Tailwind default colors / hex / px),
 *   - match its gallery specimen (variants, sizes, states),
 *   - expose a typed props contract.
 *
 * Export order will mirror the gallery: atoms → molecules → organisms.
 */

export {};
