# Evidence — State handling / unhappy paths (P-ST calibration)

Method: studied ~30 real web screens across the states F33 names but most products ship cheaply (loading, error) plus the ones F33 doesn't enumerate at all (partial failure, pending, offline, stale-while-revalidate, optimistic+rollback, success), from a reference study of real, public, shipped products, supplemented by the established server-state/resilient-UI literature for the mechanics that don't show in a still (optimistic rollback, request identity, error boundaries, SWR). Read by **(a) craft to learn** and **(b) how the rules apply** — where reality confirms a rule, calibrates a number, or shows a rule needs *scope*. The strongest reads here are two *scope* catches: error-treatment is not one thing (it scopes field → section → widget → page, and reality drew every band cleanly), and loading is not one thing (skeleton-when-layout-known vs spinner-when-unknown, also drawn cleanly). 2026-05-29.

Screens, by state:
- **Loading / skeleton (layout known)**: Neon (skeleton *table rows* matching compute columns), Vercel (Speed Insights — skeleton score-ring + chart blocks in real card slots), Quicken (skeleton sidebar + two content cards in final slots), Frame (skeleton main + right panel), Uvodo (skeleton sidebar list + spinner in unknown canvas), Linktree (skeleton header + content blocks).
- **Loading / spinner (layout unknown or transient)**: Threads (bare spinner pre-content), Canva (dimmed overlay + spinner over a *partially-laid-out* gallery — a background-refetch flavor).
- **Error + retry (full-page / region)**: Threads ("Something went wrong, please try again later." + `Retry`), VEED ("Oops, that's our bad. We're not exactly sure what happened, but our servers say something is wrong." + illustration + `Try Again`), Typeform ("Oops! Something went wrong" + small mascot + `Try again`), PayPal ("Something went wrong / This happens sometimes. Trying again may help fix the problem / Debug ID: f8482509254bd" + `Retry`), SuperHi ("Hmmm… / There was an error trying to sign you in. Please try again!" + `Try again`), Skiff ("Unable to load page / Please try again or get in touch" + `Send feedback` / `Reload` + help link), Remote (import wizard — **step-chrome intact**, content region "Sorry, something went wrong on our end / This page failed to load. Please retry or contact your Remote representative." + `Retry`).
- **Field / form inline errors**: The New Yorker (only the invalid "Credit card number" field reddened — "Your card number is invalid." — valid fields untouched), Melio ("You must be at most 120 years old" under the date field, red label + red underline), Substack (red ⓘ in-field + "Please enter a valid web URL" below), Plaid/Zillow ("Please enter a valid phone number" under field, red icon, in a modal), Aboard ("⚠ Code is invalid" under the OTP cells), Linktree (**section banner** "Password must be at least 8 characters" above the field set — a form-level rule, not a field-level one).
- **Success / confirmation toasts**: Sora ("Saved username" top-right), June ("Saved successfully / Your setup was saved successfully." toast *while the page still shows a spinner* — async confirm decoupled from page-load), Maze ("Confirmed!" + ✕, dismissible), Contra ("Project terms declined and message sent"), Lovable ("Email verified" toast + inline "Email verified / Redirecting…"), Air ("Domain verified" toast + a centered confirm card with `Close`).

## Rule-application reading

| Rule | What real screens do | Verdict |
|---|---|---|
| empty ≠ error ≠ loading (intro) | loading screens show motion/skeleton (Neon/Vercel/Quicken), error screens say "something went wrong + Retry" (Threads/PayPal/Remote) — *never* "Nothing here yet"; no studied error screen rendered an empty-state | **CONFIRM the split** — the three truths get three different treatments in the wild; conflating them (empty-for-failed-load) is the tell to forbid. |
| Skeleton when layout known, spinner when not (P-ST-01) | Neon/Vercel/Quicken/Frame/Linktree skeleton-mirror **because the layout was predetermined**; Threads/Canva spinner where the content shape wasn't | **CONFIRM + CALIBRATE → P-ST-01**: the deciding variable is *is the layout known*, not just the wait length. |
| Skeleton mirrors final layout (P-ST-02) | Vercel traced the score-ring circle + chart blocks in their real positions; Neon laid skeleton *rows* with its real column count; Quicken reserved both content-card slots | **STRONGLY CONFIRM → P-ST-02**: the value over a spinner is spatial — zero reflow on arrival. A 3-grey-bars skeleton for a 6-col table forfeits that. |
| Error scope matches failure (P-ST-03) | Remote kept full wizard step-chrome (Upload ✓/Map ✓/Review ✓/Confirm) live, errored **only the content region**; Threads/Skiff/PayPal owned the whole viewport for true route failures | **CONFIRM → P-ST-03**: scope tracks what failed — a region error keeps the rest usable, a route error takes the viewport. Wrap risky *regions*, not the app (resilient-UI literature). |
| Field vs form error scope (P-ST-04) | New Yorker reddened **only** the invalid field; Melio/Substack/Plaid/Aboard put the message *under the specific field*; Linktree used a **section banner** above the field set for a form-level rule | **CONFIRM + CALIBRATE → P-ST-04**: the red's scope = the problem's scope. Field problem → field; form problem → banner above. (P-ST-03 applied to forms.) |
| Calm error + retry, code as fine-print (P-ST-05) | PayPal "Something went wrong / This happens sometimes / **Debug ID: f8482…** / Retry" — human cause, retry primary, machine ID demoted to fine print; Threads/Remote/SuperHi all factual + retry; none joked in a hard failure | **CONFIRM → P-ST-05** (extends P-ES-03 to scoped errors): plain cause, retry primary, reference code only as fine print. |
| Tone: warmth OK only on light failures (F73) | VEED/Typeform allowed a small "Oops"+mascot on a soft app error; PayPal stayed flat on a *payment* surface | **SCOPE-CONFIRM F73** (already in P-ES): a mascot is tolerable on a light/transient failure, never on payment/data-loss. |
| Button owns the wait, result = toast (P-ST-08) | June showed "Saved successfully" toast **while the page kept working** (no reload); Sora/Maze/Air/Lovable confirmed via brief edge toasts | **CONFIRM → P-ST-08**: the async result lands as a decoupled toast/inline confirm, not a full-page reload. |
| Background refetch keeps content (P-ST-09) | Canva kept its gallery **laid out** under a dim+spinner overlay rather than blanking to skeleton | **PARTIAL-CONFIRM → P-ST-09**: content stays present during refetch (Canva's dim is a mild version; the cleaner pattern is no dim, just a refresh affordance). |
| Proportional success (P-ST-11) | every routine save/confirm was a *brief toast* (June/Sora/Maze/Contra/Air/Lovable) — none used a full screen for a routine action | **CONFIRM → P-ST-11**: routine → toast; full success screen reserved for milestone completion (checkout/onboarding, per P-CK / P-ES first-run). |
| F69 problem+fix / F73 no jargon | "Enter a valid web URL", "You must be at most 120 years old", "Please try again or get in touch"; PayPal's code hidden as Debug ID | CONFIRM: plain-language cause+fix; system vocab kept out of user-facing copy. |
| F32/F68 verb labels on retry | `Retry`, `Try Again`, `Reload`, `Send feedback` — all verb-labelled | CONFIRM: the recovery action names its verb. |

## Mechanics not visible in a still (literature-grounded, flagged)
- **Optimistic UI + rollback (P-ST-07)** can't be captured from a screenshot. Grounded in server-state practice: apply instantly, reconcile on response, snapshot-rollback on failure; assign each mutation a **request identity** so only the latest commits/rolls back and stale responses are ignored; a failed mutation must roll back *and tell the user* (a quiet toast — never a silent revert). The line: reversible-and-cheap ⇒ optimistic; consequential-or-slow ⇒ pending-then-confirm. Flagged in the pattern as literature-grounded, not screenshot-observed.
- **Partial failure (P-ST-06)** rarely shows in a single curated capture (you'd need a multi-fetch surface with one fetch down). Grounded in resilient-UI practice: wrap risky *regions* in lightweight boundaries showing a local fallback + retry that doesn't block the page; a *partial rollback* (keep the change visible but marked "failed, retry") is sometimes friendlier than a hard revert. Reasoned from P-ST-03's confirmed scope finding + the literature.
- **Stale-while-revalidate (P-ST-09)** swap-in-place and "never persist known-stale" is an SWR/Query contract (revalidation ditches the in-flight request and never displays stale once fresh arrives). Canva is the closest still; the rule's "never re-skeleton a populated screen" is grounded there + DVLA's SW `NetworkOnly`-for-API lesson (never blanket-cache API GETs).

## The dynamic-generation lesson
State handling varies by **what is true and what failed, not a template**: the *truth* (loading / empty / error / offline / stale) picks which treatment; the *scope of the failure* (field / region / route) picks how big the error is; the *expected wait + known-ness of the layout* picks skeleton-vs-spinner-vs-optimistic; the *reversibility + stakes* of a mutation picks optimistic-vs-pending; the *weight of the action* picks toast-vs-screen for success. The constant is the discipline — every state is truthful, correctly scoped, recoverable, and announced (`aria-live`) — but the treatment flexes to the situation. Stamping one "loading spinner" + one "error page" across every surface is the tell, the same way one generic "empty" component is the P-ES tell.

## Over-strict / wrong-rule catches (the strongest calibrations)
- **Loading is not one treatment — skeleton-vs-spinner is a *decision*, gated on whether the layout is known.** A naive "always use a skeleton" would have flagged Threads/Canva's correct spinner (unknown content shape); "always spinner" would have lost the zero-reflow win Vercel/Neon/Quicken got from layout-mirroring skeletons. The deciding variable reality showed is *is the layout predetermined* (+ wait length), not a flat rule. → P-ST-01/02.
- **Error is not one scope.** Reality drew four bands cleanly: New Yorker (field), Linktree (section banner), Remote (region with chrome intact), Threads/PayPal (full page). A flat "show an error page" would over-scope a field validation into a viewport-blanking interstitial. → P-ST-03/04/05, mirroring P-ES-10's scope finding.
- **Background refetch must not re-skeleton.** Canva keeping its gallery laid out (vs blanking to grey) shows the right instinct; the forbidden move is the regression of taking readable content away to show skeletons on every revalidation. → P-ST-09.

## Gaps / reasoned-not-observed
- **Partial failure** and **optimistic rollback** are mechanics, not stills — reasoned from P-ST-03's confirmed scope finding + server-state/resilient-UI literature, flagged as such in the pattern (P-ST-06/07).
- **Offline degraded-but-usable banner (P-ST-10)** — P-ES already captured the *full-page* offline moment (Quicken/Udemy); the in-app auto-retrying banner over still-usable content is reasoned from F73 (transient, not the user's fault) + the resilient-UI "keep content usable" principle, deferring the full-page case to P-ES-06.

## Calibrations applied to patterns/states.md
1. **empty ≠ error ≠ loading** truth-split is the spine + the #1 forbidden move (empty-for-failed-load).
2. Loading → **skeleton when layout known + wait ~300ms+, spinner when unknown/tiny/short, optimistic under ~100–300ms** (P-ST-01); **skeleton mirrors the layout** (P-ST-02).
3. Error → **scope matches failure** (field/section/region/page — P-ST-03/04), **calm + retry + code-as-fine-print** (P-ST-05, extends P-ES-03).
4. **Partial failure** is its own state — render what loaded, scope the failure + retry (P-ST-06).
5. Mutations → **optimistic+visible-rollback for reversible-cheap; pending+confirm for consequential-slow** (P-ST-07); **trigger owns the wait, result = toast** (P-ST-08).
6. **Stale-while-revalidate** keeps content live, swaps in place, never persists stale (P-ST-09).
7. **Offline** = unobtrusive auto-retrying banner, content usable (P-ST-10, defers full-page to P-ES-06).
8. **Success proportional to stakes** — toast / inline / screen (P-ST-11).
9. **a11y announce + reduced-motion** on every state change (P-ST-12, Tier-1).
