# Evidence — CRUD list / index page (P-L calibration)

Method: studied 14 real web list/index pages across six domains (admin data-grid, users/members management, orders/transactions, knowledge articles, agent-task queue, project/file galleries), read by **(a) craft to learn** and **(b) how the rules are applied** — where reality confirms a rule, calibrates a number, or reveals one as too strict / mis-scoped. Reading reality is what catches the over-strict guess; the strongest calibrations below are rules that real screens *bent*.

Screens: Braintrust (playground data-grid), HubSpot (Lists — Manage), Salesforce (Knowledge — All Articles), Relevance AI (Tasks + filter popover), Mixpanel (Users), Asana (Admin Users — 2 selected bulk bar), User Interviews (Designers — row overflow + select-all), monday.com (people-100 colored-cell grid), Squarespace (Orders), Shopify (Orders — list view + 1-selected bulk bar), Time2book (Catalog — Orders tab), Programa (Projects card grid), Dovetail (project-template picker grid), Figma (Team project — file grid + view toggle). 2026-05-29.

## Rule-application reading

| Rule | What real list pages do | Verdict |
|---|---|---|
| Toolbar shape (P-L-01) | search + filter + sort + a **single** primary top-right: HubSpot "Create list", Shopify "Create order", Squarespace "Export data", Asana "Invite users", Programa "Create New Project", Figma "Create new". Search is a left-of-toolbar field (HubSpot, Mixpanel, Shopify) or an icon that expands (Salesforce, Programa). | **CALIBRATE → P-L-01**: one toolbar row — search anchored left, filter/sort/column-controls center-right, one primary action far right. Universal across all six domains. |
| Action soup (P-L-01 forbidden) | **Salesforce "All Articles"** crams New · Publish · Assign · Archive · Delete Article as five equal-weight buttons in the toolbar | **CALIBRATE → P-L-01 forbidden move**: that five-equal-button strip is the list-page face of AP18 — only "New" earns the toolbar; verbs that act on *selected rows* belong in the bulk-action bar, not the resting toolbar. The cautionary case. |
| Quick-filter tabs (P-L-02) | Status tabs above the table: Shopify All/Unfulfilled/Unpaid/Open/Closed; HubSpot All lists/Unused lists/Recently deleted; Salesforce article tabs; Time2book Services/Plans/Coupons/Orders | **NEW → P-L-02**: a saved-view / status tab strip is the most common coarse filter, sitting *between* the page title and the toolbar. Distinct from sub-navigation — these slice the same collection. |
| Table vs card grid (P-L-03) | **Tables** for data records: HubSpot, Salesforce, Shopify, Squarespace, Mixpanel, Asana, Time2book, Relevance AI. **Card grids** for visual/thumbnail records: Programa (interior projects w/ cover photo), Figma (file thumbnails), Dovetail (template covers), [untitled] (video projects). | **NEW → P-L-03 (contextual)**: the table↔card choice tracks whether the record's identity is *visual*. A thumbnail-bearing record (design file, project, media) earns a card; a data row (order, user, article) earns a table. This IS the dynamic-generation branch. |
| Column weight (P-L-04) | Shopify Order column = bold link + secondary date beneath; Customer is a name+avatar; Total/Status carry weight; Channel/Delivery are muted tail columns | **NEW → P-L-04**: columns are not equal-weight — an identity column (linked, bold), 2–4 facts that earn their place, status as a pill, numerics right-aligned; the long tail stays muted. Flat equal columns = the table face of AP8/AP20. |
| Numerics (F24) | Squarespace TOTAL right-aligned; Shopify Total $18.90; Time2book Amount $49.00; Mixpanel numeric cols | **CONFIRM F24**: currency/counts right-align with tabular figures; decimals align. None left-aligned a money column. |
| Row affordance (P-L-05) | First-column link (Shopify #1001, Salesforce article #, HubSpot "Open Tickets"); explicit **View** button (User Interviews); per-row **⋯ overflow** menu (Asana, User Interviews Rename/Edit/Duplicate/Delete, Time2book, Squarespace, [untitled]) | **NEW → P-L-05 (contextual)**: the identity cell links to the record (whole-row click is the implicit default); a *single* secondary per-row action can be explicit; everything else collapses into a per-row ⋯ menu. Never a row of inline action buttons per row. |
| Selection + bulk bar (P-L-06) | Header checkbox = select-all; per-row checkbox; **Asana** floating bottom pill "2 selected \| Manage invites \| Change status \| ✕"; **Shopify** "1 selected" + Create labels/Mark fulfilled/Capture payments + ⋯; selection count shown ("3 of 3 showing", "1 item selected") | **NEW → P-L-06**: bulk actions appear *only on selection*, in a bar/pill that replaces or floats over the toolbar, carrying the selection count + the verbs that act on the set + a clear-selection ✕. This is where Salesforce's toolbar verbs *should* have lived. |
| Filter surface (P-L-07) | **Popover**: Relevance AI ("Where status / Is not / Completed" panel w/ Apply/Clear), Mixpanel (+Filter → chips). **Inline chips**: HubSpot (All creators ▾ / All types ▾ / All objects ▾). **No left rail** on any of the 14 admin lists | **NEW → P-L-07 (contextual)**: 1–3 common filters live inline as dropdown chips; a richer filter builder lives behind a "Filter" popover with Apply/Clear. A left filter *rail* is for filter-heavy catalog/search surfaces, not the everyday admin list — none of the studied admin lists used one. |
| Pagination (P-L-08) | "10 per page ▾" + ‹ › (Squarespace); "25 per page" + Prev 1 Next (HubSpot); "Showing 1–4 of 4" + page arrows (User Interviews); long task/people grids (Relevance AI, monday.com) just scroll | **NEW → P-L-08 (contextual)**: explicit page-size + prev/next is the admin-table default (the count is load-bearing); long homogeneous feeds (tasks, rows-as-spreadsheet) scroll/virtualize. Pick by whether the user navigates *to a page* or *scans a stream*. |
| Density (F26/F27) | 32–40px rows, hairline separators, 13px type (HubSpot, Shopify, Squarespace, Mixpanel); card grids loosen to medium | **CONFIRM compact band** — the list inherits the dashboard's compact density (it's the same scan-first product surface). Card grids relax one notch. |
| Borders / hierarchy (F13/F21) | Row separation = **hairlines** (or zebra), never boxed rows; toolbar separated by gap; cards = 1px neutral border + soft shadow | **CONFIRM**: the Tier-1 exception in fundamentals (a dense table genuinely needs row dividers) — lightest hairline, per F21. |
| Empty-value cells (the caution) | **Mixpanel** renders a full grid of "undefined" / "(not set)"; **monday.com** colors Male/Female cells green/orange | **REVEALS two tells**: (1) raw "undefined"/null leaking into cells (F72/F73 — format or show "—"); (2) color as decoration on non-status data (F19/F44 — monday's gender cells aren't status). Both fed forbidden moves. |
| States (F33) | (Mostly populated captures.) User Interviews "Showing 1–4 of 4"; HubSpot "2 Lists" count under title | **APPLY → P-L-09**: a list has *two distinct* empties — first-run (no records ever) vs filtered-no-match — plus skeleton rows + a scoped error; the count belongs near the title. Captures rarely show empties → the strongest reason to mandate them. |

## The dynamic-generation lesson
List pages vary on **two independent axes**: (1) **table vs card grid** — does the record's identity read visually (a design file, a project with a cover, media → card) or as structured facts (an order, a user, an article → table); and (2) **paginate vs scroll** — does the user navigate to a *page* of records (orders, articles → numbered pagination with a count) or scan a continuous *stream* (tasks, a spreadsheet of rows → virtualized scroll). The constant is the *anatomy* — title + count, a quick-filter/saved-view strip, a one-row toolbar (search-left, filter/sort-center, one primary-right), the records, a selection→bulk-bar, and a pager. The table-vs-card and paginate-vs-scroll picks flex to the content; getting them right is the proof the layout was reasoned from the records, not stamped from a template.

## Over-strict / wrong-rule catches (the strongest calibrations)
- **Salesforce's five-button toolbar** (New/Publish/Assign/Archive/Delete) showed that a list toolbar accretes verbs that actually act on *selected rows* — they belong in the bulk-action bar (P-L-06), not the resting toolbar. Without seeing it, the naive rule would have allowed a multi-button toolbar. Fed P-L-01's "one primary only" + the action-soup forbidden move.
- **No admin list used a left filter rail.** The instinct to give a list page a faceted left rail (carried over from e-commerce search) is wrong for the everyday admin index — HubSpot/Shopify/Asana/Mixpanel all use inline chips or a popover. Fed P-L-07's "rail only when filter-heavy; inline/popover otherwise" and the forbidden "filter rail for 2 filters."
- **Mixpanel's wall of "undefined"** and **monday's colored gender cells** proved the failure isn't layout — it's letting raw null leak through (format it, or "—") and spending status-color on non-status data. Fed the forbidden moves and tie them back to F72/F19.

## Run — live-UI calibration pass 2026-05-29

Method: a fresh **reference study of real, public, shipped products** — ~30 web list/table screens read rule-by-rule (VALIDATE / CALIBRATE / TOO-STRICT). Breadth across the same six domains plus payments/transaction lists and file/project card grids. Goal of this pass: re-test the nine P-L rules against a *second, larger* sample to see whether any number drifts or any rule proves too strict. Discipline: refine-don't-add.

Screens (all real public products): Eventbrite (Order Management + row ⋯ menu + filtered single-result), HubSpot (Orders grid w/ Edit columns; Lists filtered-empty "No contacts match these filters"), Squarespace (Orders — tabs, sort menu, Filter **right-drawer**, applied filter chips, per-tab empties, 10/page + "1–4 of N"; Pay Links — paginated), Shopify (Orders — KPI strip + tabs + 1-selected floating bulk bar + saved-search "Add filter"), Lemon Squeezy (Orders — chart + KPI tiles + table + date-range popover), Wix (Orders — first-run onboarding banner + Filter + bulk-select bar), Coinbase (dark Orders — inline filter chips + "Cancel all"), Square (All orders — tabs + inline filter chips), Fiverr (Manage Orders — status tabs w/ counts), Uvodo (minimal Orders — count beside title), Deel (Bulk edit — all-rows checked + inline filter chips + bottom Continue), Clay (Find Companies Table — 25/25 rows selected → top-right "Blue actions ▾" w/ destructive Delete in red), Asana (Users — bottom floating "N selected | Manage invites | Change status | ✕"; Remove confirm dialog; Files card grid; Filters:2 applied chip), folk (All applicants — bottom floating bulk bar w/ overflow ⋯), Sprig (Users — bottom bulk pill "5 users | Add to Manual Group | ✕"; toast confirm), Aboard (People — applied filter chips + bottom "N selected · Actions ▾" single-menu), Pipedrive (Leads — **right-side bulk-edit panel** "2 SELECTED" w/ field-by-field "Keep current value"), monday.com (Users — full-width bottom blue bar "1 User selected" + Files Gallery card/list toggle), 1Password (People — full-width blue bar replacing footer + "Actions available ▾" w/ disabled/enabled verbs + Suspending… toast; "Results per page 25 ▾ / 1 of 1"), Salesforce (Leads — per-row caret ▾, sortable column carets), Podia (Audience — per-row ⋯ Edit/Reset password/.../Delete), User Interviews (Designers — per-row View + segment ⋯ + "Showing 1–4 of 4" + page arrows), Customer.io (People — "1–27 of 27" + first/prev/next/last), Whop (Payments — inline filter chips + Edit columns panel + "Showing 1 to 2 of 2" + ‹›), Apollo (Tasks **filtered-empty** "No tasks here! …clear any applied filters" + **Clear filters** btn; Meetings empty "No meetings found / adjusting your search or filters / Reset filters"), Navan (Trips **filtered-empty** "There are no trips" w/ filter chips retained), Fresha (Clients **empty** "No clients found / Try adjusting your search criteria" boxed), Zapier (filtered-empty "No results found / Ensure data exists… / Refresh data"), Qatalog (Pages filtered-empty "No Pages found / update the filters"), Google Classroom (To-do **first-run** "Nothing on your to-do list right now" warm illustration; People — checkbox + "Actions ▾" single menu), Lovable / Figma / Frame.io (dark) / Adobe Express / Basecamp / MasterClass (visual-record **card grids** — cover + title + 1–2 meta + ⋯, 3-up, "+ Create"/"New" tile).

### Rule-application reading

| Rule | What this larger sample showed | Verdict |
|---|---|---|
| P-L-01 toolbar shape | One row, one primary far right, search left: HubSpot "Create order", Shopify "Create order", Squarespace "Export data"/"Create new", Wix "Add New Order", Eventbrite "+ Create", Lovable "Create new project", Figma "Create new". Column-visibility ("Edit columns"/"Show or hide columns") is a real recurring center-right control (HubSpot, Shopify, Whop, Clay). | **VALIDATE.** Universal again at 2× sample; "Edit columns" confirms the "column-controls" clause already in the rule. No change. |
| P-L-02 quick-filter tabs | Status tabs above the table once more: Shopify All/Unfulfilled/Unpaid/Open/Closed, Squarespace All Statuses/Pending/Fulfilled/Canceled, Square All/Active/Scheduled/Completed/Canceled, Fiverr Priority/Active/Late/… **with counts on tabs**, Asana All/Members/Guests/Invited. Omitted on collections w/ no canonical slice (Uvodo, Coinbase, Customer.io). | **VALIDATE.** The "count may ride the tab" clause corroborated by Fiverr/Shopify. No change. |
| P-L-03 table vs card | Tables for all data lists; card grids for visual records — Lovable projects, Figma files, Frame.io media (dark theme), Adobe Express, Basecamp, MasterClass, Asana Files. 3-up, cover + title + 1–2 meta + ⋯, "+ Create"/"New" first tile. Frame.io proves the card grid is theme-agnostic (dark). | **VALIDATE.** No change. |
| P-L-04 column hierarchy | Shopify/Squarespace identity = bold linked Order# + date beneath; Customer = name+avatar; Total/Status carry weight; Channel/Delivery muted. Salesforce Name is the linked identity col. Status as tinted pill (Paid/Fulfilled/Refunded). | **VALIDATE.** No change. |
| P-L-05 row affordance | Identity-cell link + whole-row click default; ONE explicit secondary at most (User Interviews "View"); everything else in a per-row ⋯ (Podia, Eventbrite, User Interviews) or a per-row caret ▾ (Salesforce). No app showed a row of inline action buttons. | **VALIDATE.** Salesforce's per-row ▾ is the same device as ⋯ (a single collapsed menu) — already covered. No change. |
| P-L-06 selection → bulk bar | Bulk verbs appear ONLY on selection, every time. **Placement: bottom-floating bar/pill dominates** (Asana, Shopify, folk, Sprig, Aboard, monday, 1Password, Lovable). Two other shapes seen: **right-side edit panel** for field-by-field bulk *edit* (Pipedrive "2 SELECTED" → Keep-current-value per field) and **top-right anchored** "Actions ▾" next to the row count (Clay). Selection count + clear-✕ present throughout. | **VALIDATE + minor CALIBRATE.** The rule already allows "floating bottom or replacing the toolbar"; add the **right-side panel** as the recognized third placement for bulk-*edit*, and note **top-right-anchored** as a valid alt. |
| P-L-06 (bulk verb count) | When many set-verbs exist, excellent apps collapse them into ONE "Actions ▾" menu (Aboard, Google Classroom, Clay "Blue actions", 1Password "Actions available") rather than a row of 4–5 bulk buttons. Asana/Shopify show 2–3 buttons inline only because the set is small. | **CALIBRATE → P-L-06.** Extend the one-primary logic into the bulk bar: 1–3 set-verbs may sit inline; **beyond that, collapse into a single "Actions ▾" menu** (the bulk-bar face of P-L-01's anti-soup). Destructive in the menu, confirmed (Asana Remove dialog, Clay red Delete). |
| P-L-07 filter surface | Inline removable chips dominate (Navan, Coinbase, Square, Whop, Fresha "Male ×/Fresha accounts ×/Clear all", Aboard, Squarespace applied chips). Builder behind a control: Squarespace's "Filter" opens a **right-side drawer** (not a small popover); HubSpot filter builder. **Zero left filter rails on any admin list across ~30 screens** (the only rails were faceted catalogs: Uniswap/OpenSea NFT marketplaces — exactly the carve-out). | **VALIDATE + tiny CALIBRATE.** Add that the richer-filter container is a **popover *or right-side drawer*** (Squarespace ships the drawer). The "no left rail for admin lists; rail only for faceted catalog/search" rule is now corroborated by ~30 screens incl. the two genuine faceted exceptions. |
| P-L-08 paginate vs scroll | Explicit pager + load-bearing count once more: Squarespace "10 per page ▾ + ‹› + Showing 1–4 of N", HubSpot "25 per page + Prev 1 Next", User Interviews "Showing 1–4 of 4", 1Password "Results per page 25 ▾ / 1 of 1", Customer.io "1–27 of 27 + first/prev/next/last", Whop "Showing 1 to 2 of 2 + ‹›". Streams/grids scroll (Clay, monday, Sprig, folk, Customer.io people-stream). | **VALIDATE.** The first/prev/next/last (jump-to-ends) variant on long sets (Customer.io, 1Password) is a refinement of "prev/next" — already within the rule's spirit. No change. |
| P-L-09 states (the headline of this pass) | **Two-empties finally captured at scale.** Filtered-no-match: HubSpot "No contacts match these filters / Try changing your filters", Apollo "No tasks here! …clear any applied filters" + **Clear filters btn**, Navan "There are no trips" (chips retained), Fresha "No clients found / Try adjusting your search criteria", Zapier "No results found / …Refresh data", Qatalog "No Pages found / update the filters". First-run/warm: Google Classroom "Nothing on your to-do list right now" + illustration; Wix first-run onboarding banner over the table. **None of the filtered-empties showed a "Create your first X" CTA** — they offer clear-filters/adjust-search. Confirmed live exactly as P-L-09 mandates. | **VALIDATE (strongly).** The prior pass had to *infer* this from mostly-populated captures; this pass has the receipts. The filtered-empty's escape affordance is reliably "clear/adjust filters", never the onboarding CTA. No change — the rule was right. |
| P-L-10 format cells / no decoration color | Status pills are the only color in well-built grids (Shopify Paid/Fulfilled green tints, Squarespace Pending amber). Uvodo/Customer.io render relative time ("2 hours ago", "in 3 years") not raw timestamps. No "undefined" leak in the polished examples (the prior pass's Mixpanel/monday tells stand as the cautionary cases). | **VALIDATE.** No change. |

### Over-strict / wrong-rule catches
- **None this pass.** No P-L rule was broken by an excellent screen without cost. The closest thing to a stretch — Pipedrive's right-side bulk-edit panel and Clay's top-right "Actions" — are *placement variants* of P-L-06, not violations; they're folded in as recognized alternates rather than new rules.
- The **bulk-verb-count** finding (collapse >3 set-verbs into one "Actions ▾") is the one substantive refinement: it's not a new rule, it's extending P-L-01's anti-soup discipline into the bulk bar, which P-L-06 already gestures at ("one primary action in the bulk bar too").

### Convergence note
This is a second-sample re-test of an already-calibrated pattern. Nine of ten rules VALIDATE unchanged; P-L-06 gets two small placement/affordance refinements (no behavior reversal); zero new rules; zero too-strict catches. The pattern reads as **converged against current reality** — the only edits proposed are absorbing observed placement variants into P-L-06's existing language.

## Calibrations applied to patterns/list.md
1. Toolbar → **one row: search-left, filter/sort/columns-center, one primary "New X"-right**; verbs-on-rows go to the bulk bar (P-L-01).
2. Quick-filter / saved-view **tab strip between title and toolbar** (P-L-02).
3. **Table vs card grid contextual** on visual-vs-structured identity — the dynamic branch (P-L-03).
4. **Columns carry hierarchy** — identity link, 2–4 earning facts, status pill, right-aligned numerics, muted tail (P-L-04).
5. Row affordance → **identity links + whole-row click; one explicit secondary; rest in a ⋯ menu** (P-L-05).
6. **Selection reveals a bulk-action bar** with count + set-verbs + clear-✕ (P-L-06).
7. Filters → **inline chips for 1–3; popover builder for richer; rail only when filter-heavy** (P-L-07).
8. **Paginate (with count) vs infinite-scroll/load-more contextual** on navigate-to-page vs scan-stream (P-L-08).
9. **Full state set** — skeleton rows, first-run empty ≠ filtered-no-match empty, scoped error (P-L-09).
