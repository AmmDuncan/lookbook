# Evidence — Mobile (reflow grammar + mobile-native compositions) (P-MO calibration)

Method: studied 21 real **mobile** (iOS) screens across six composition classes — bottom-tab navigation, hamburger/drawer navigation, filter bottom sheets, bottom-sheet modals, mobile detail screens, mobile lists — read by **(a) craft to learn** and **(b) how the rules are applied** — where reality confirms a rule, calibrates a number, or reveals a desktop assumption that doesn't survive a 390px touch surface. Reading reality on the phone is what catches the over-strict desktop guess; the strongest calibrations below are the components that *don't exist on desktop* and the desktop rails/dialogs that real mobile UI **refuses** to cram in. 2026-05-29.

Screens: **Bottom-tab nav** — Glass (floating pill bar + separated `+`), Airbuds Widget (4 icon-only tabs), Attio (4 tabs w/ labels: Home/Search/Tasks/Notifications), Duolingo (5 tabs + `⋯` overflow). **Drawer nav** — DoorDash Dasher (8-item icon+label drawer over a map), GCOO (8-item right-side drawer), Forest (12-item left drawer w/ scrim). **Filter sheet** — Beli (Sort-by segmented + collapsible facets + `Clear all` / `Apply (3)`), Instagram (Sort & filter sheet + `Clear` / `View 26 products`), Mercury (checkable facet rows + `See Results` / `Clear Filters (4)`), eBay (`Selected (1)` chip + chip-group facets + `Clear all` / `Show results`). **Bottom-sheet modal** — Skip (Menu Categories picker + `Cancel`), Tinder (near-full-height "Add birth place" search sheet w/ `X`), eBay (About-this-item detail sheet w/ `X` + scrim), Revolut Business (reason picker, searchable, dark, drag-handle). **Mobile detail** — Viator (hero image → title → price/rating → sticky `From $25.65 \| Check availability` bar), Me+ (article → single sticky `+ Add to my routine` pill), Alma (recipe → macros → two sticky actions: ghost `Share` + primary `Track`), Keeta (item sheet → sticky `– 1 + \| Add to cart ₿48.00`). **Mobile list** — Todoist (search-first + scrollable filter-chip strip), Target (type picker, checkbox rows, sticky `See results`), Notion (search list + `Sort ▾` + `Add filter` + a quick-filter sheet).

## Rule-application reading

| Rule | What real mobile screens do | Verdict |
|---|---|---|
| Bottom tab bar (P-MO-06) | Attio (Home/Search/Tasks/Notifications, icon+label), Airbuds (4 icon-only), Duolingo (**5 tabs + a `⋯` More**), Glass (floating pill, 4 icons + a *separated* `+` create) — all pinned to the bottom edge, thumb height | **NEW → P-MO-06**: 3–5 flat, frequently-visited destinations as a bottom tab bar; ≥5th destination folds into a `⋯`/More tab (Duolingo) rather than shrinking targets. A create/compose action that isn't a destination sits *apart* (Glass's `+`), not as a peer tab. |
| Drawer vs tabs (P-MO-06 contextual) | DoorDash Dasher (8: Home/Schedule/Account/Ratings/Earnings/Promos/Preferences/Help), GCOO (8), Forest (**12**: Forest/Challenge/Timeline/Tags/Friends/Achievements/Store/News/Settings…) — all use a slide-in **drawer with a scrim**, opened from a hamburger | **CALIBRATE → P-MO-06 (the dynamic branch)**: when destinations are **many (>5) or deep/infrequent** (settings, earnings, help), a drawer is correct — a tab bar would either drop them or shrink below 44px. Tabs = flat & frequent; drawer = deep & many. Neither is "the mobile nav"; the count + frequency picks. |
| Filter → bottom sheet (P-MO-08) | Beli, Instagram, Mercury, eBay all open filtering as a **bottom sheet** — drag handle, `Filters` title, collapsible facet rows, and a **sticky footer pair**: `Clear all` (text/ghost, left) + `Apply (3)` / `Show results` / `See Results` (filled, right, **carrying the result/selection count**) | **NEW → P-MO-08**: mobile filtering is a sheet with a sticky Clear/Apply footer, never a desktop rail squeezed into 390px. The count on the primary ("Apply (3)", "View 26 products") is near-universal — it tells the user what the apply will do. eBay's `Selected (1)` chip + chip-group facets shows multi-select state inside the sheet. |
| Modal → bottom sheet (P-MO-09) | Short pickers anchor a **partial sheet** (Skip Menu-Categories + `Cancel`, Revolut reason picker); a **search/long-content task takes a near-full-height sheet** (Tinder "Add birth place", eBay "About this item" detail) — all carry a **drag handle**; X/Cancel dismiss; the page behind **dims (scrim)** | **NEW → P-MO-09 (contextual on content height)**: short/bounded content → partial bottom sheet with handle; tall/search/scroll content → near-full-height sheet (still a sheet, with handle + dismiss); a true blocking decision can stay a centered dialog. Every sheet had a visible dismiss + scrim — never a trap. |
| Thumb-zone sticky action (P-MO-10) | Viator (`From $25.65 \| Check availability` sticky bar), Keeta (`– 1 + \| Add to cart ₿48.00`), Me+ (single `+ Add to my routine` pill), Alma (ghost `Share` + primary `Track`), and every filter sheet's `Apply` — the **primary action lives in a sticky bottom bar within thumb reach**, price/context on the left, the one filled verb on the right | **NEW → P-MO-10**: the mobile primary belongs in a sticky bottom bar/FAB, not scrolled away at the top. Two-action bars keep exactly **one filled** (Alma: ghost Share + filled Track) — F16 holds on mobile. Destructive actions are *not* placed in the thumb bar (none of the screens did) — they live behind `⋯` (Alma's Rename/Delete popover, away from the bottom). |
| Touch targets (F52) | Tab-bar items, drawer rows, facet rows, sticky-bar buttons all read as **≥44px tall** with clear spacing; Duolingo folds a 6th destination into `⋯` rather than shrinking five into a sixth slot | **CONFIRM F52 at every width**: real mobile UI grows the row/target to the finger, and *removes* destinations (overflow) before it shrinks them. The failure isn't visible here precisely because shipped apps don't do it. |
| Detail reflow (P-MO-03 / from P-DT) | Viator/Alma/Keeta detail = **single column**: hero/title → key facts (price, rating, macros as a compact strip) → body → sticky action bar. No two-column, no side rail; the desktop right-rail's facts became the top strip + the sticky bar | **APPLY → P-MO-03**: the desktop two-column detail collapses to stacked single column; the summary rail's *primary* fact (price) is promoted into the sticky bottom bar, the rest into a top key-facts strip. Nothing dropped. |
| List reflow (P-MO-04 / from P-L) | Todoist/Notion/Target list = **search-first condensed toolbar** (search field + a `Sort ▾` + `Add filter` link or a scrollable chip strip), rows as full-width stacked items, filters behind a sheet | **APPLY → P-MO-04**: the desktop list toolbar condenses — search stays, filter/sort collapse to a chip strip or a sheet trigger; the table either horizontal-scrolls or each row stacks (identity + meta). |
| Content parity / mobile-first (F50, F51) | Across all 21 screens nothing essential is hidden — desktop affordances are *relocated* (rail→sheet, top primary→bottom bar, tab overflow→`⋯`), not dropped; breakpoints are content-driven (the column collapses when it can't hold its measure) | **CONFIRM F50/F51**: mobile-native design *moves* affordances into thumb-and-sheet idioms; it never `display:none`s content. This is the whole reflow grammar. |
| Fluid type/space (F53) | (Static captures.) Type holds proportion; sheet/​bar padding ≈ 16–20px; titles ~22–28px | **APPLY F53**: type & space scale via `clamp()` between the mobile and desktop ends; the mobile end is the design's starting point (F50). |

## The tablet / medium band (~600–1024px) — the most-broken width
The reference study above is **phone-only** (iOS, ~390px); the mobile reference captures don't exercise the ~768–1024px tablet band, so P-MO-12 is calibrated from the content-driven-breakpoint principle (F51) + responsive practice rather than a tablet receipt — flagged honestly. The reasoning: every collapse in the table above (rail→sheet, top-primary→bottom-bar, nav→drawer, multi-col→single) is the *phone* endpoint of a transform that, applied to ~768px, would over-collapse — a 900px viewport does **not** want a single 320px column, a bottom sheet, or a hamburger when it has room for two columns, a top-of-content panel, and a condensed bar. The failure is real and ubiquitous precisely because teams test desktop + phone and let the tablet band fall through to whichever neighbour's CSS wins, yielding "desktop squeezed" (columns collide, prose > 75ch breaking F5) or "phone stretched" (one narrow column marooned in whitespace). F51 resolves it: a breakpoint lands where a *rule* breaks, and the rule that breaks at phone width breaks **differently and earlier** at tablet → the tablet band earns its own breakpoint + its own intermediate composition (3→2→1 columns, rail-to-top before rail-to-sheet, nav-condensed before nav-hamburger, grids stepped not snapped). Fed P-MO-12.

## The dynamic-generation lesson
Mobile is **not** "the desktop layout at 390px." Two things change. First, the **reflow grammar**: every desktop composition has a canonical mobile collapse — two-column detail → stacked single column with the rail's *primary* fact promoted to a sticky bar; list toolbar → condensed search + a filter sheet; dashboard table → cards or horizontal-scroll; pricing tiers → stacked, recommended first; checkout summary → a collapsible sticky total. Second, **mobile grows components desktop doesn't have**: the bottom tab bar, the slide-in drawer, the filter sheet, the bottom-sheet modal, the sticky thumb-zone action bar. The contextual picks are real: **tabs vs drawer** by destination count + frequency (3–5 flat/frequent → tabs; >5 or deep/infrequent → drawer); **partial vs full-height sheet** by content height; **sticky bar vs FAB** by whether the primary needs context (price) beside it. Getting these right is the proof the screen was reasoned for the thumb and the viewport, not shrunk from a desktop comp.

## Over-strict / wrong-rule catches (the strongest calibrations)
- **Duolingo's `⋯` More tab and Glass's separated `+`** showed that "3–5 tabs" isn't a hard cap to violate by cramming — the *overflow* destination folds into a More tab and a non-destination create action sits *apart*, so the five thumb targets stay ≥44px. Fed P-MO-06's overflow rule + "create-action isn't a tab."
- **No filter screen used a desktop rail.** Beli/Instagram/Mercury/eBay every one opened a *sheet* with a sticky Clear/Apply footer — the instinct to port the desktop left-rail filter into mobile is wrong. Fed P-MO-08 + the forbidden "desktop rail stuffed into 375px."
- **Every bottom sheet had a drag handle, a scrim, and a visible dismiss** (X/Cancel) — none were traps. Fed P-MO-09's hard requirement that a sheet is always dismissible, and the forbidden "bottom sheet with no dismiss."
- **The sticky-bar primary always carried its context** (Viator price, Keeta total+stepper, filter-sheet result count) and never the destructive action. Fed P-MO-10's "primary in the thumb bar with context; destructive away from it."

## Calibrations applied to patterns/mobile.md
1. **Mobile-first reflow + content parity** — affordances relocate, never `display:none` (P-MO-01, F50/F51).
2. **Per-archetype collapse recipes** — detail→stacked+sticky-bar, list→condensed+sheet, dashboard-table→cards/h-scroll, pricing→stacked-recommended-first, checkout→collapsible-sticky-total (P-MO-02/03/04/05).
3. **Bottom tab bar (3–5 flat/frequent) vs drawer (>5/deep) vs top-back-stack** — contextual; overflow→`⋯`, create-action sits apart (P-MO-06).
4. **Filter as a bottom sheet** with collapsible facets + sticky `Clear` / `Apply (N)` footer, never a rail (P-MO-08).
5. **Modal → partial vs full-height bottom sheet** by content; drag handle + scrim + dismiss always (P-MO-09).
6. **Sticky thumb-zone primary** (bar/FAB) with context on the left, one filled verb; destructive away from the thumb (P-MO-10).
7. **Touch targets ≥44px at every width**; overflow destinations before shrinking them (P-MO-07, F52).

---

## Run 2 — 2026-05-29 (confirmation pass + tablet / web-responsive band)

Method: a second reference study of real, public, shipped products, deliberately weighted toward (a) the two areas the first run flagged as thin — the **tablet / medium band** (read off responsive *web* products, which actually exercise ~600–1024px, where the phone-only iOS captures couldn't) and the **bottom-sheet sizing/snap** vocabulary — and (b) a re-confirmation sweep of the bottom-tab / drawer / filter-sheet / sticky-bar / detail-collapse classes to test whether Run 1's calibrations still hold against a *fresh, non-overlapping* set of screens. Read rule-by-rule (VALIDATE / CALIBRATE / TOO-STRICT). 2026-05-29.

Screens studied (real, public, shipped):
- **Bottom-tab nav (re-confirm)** — Vinted (5 tabs incl. centered `+ Sell`), Etsy (5 tabs: Home/Favourites/Search/You/Cart — *no* create-as-tab; selling lives elsewhere), Airbnb (Explore/Wishlists/Trips/Inbox/Profile, icon+label), Beli (Feed/Your Lists/**centered Search**/Leaderboard/Profile), Calm (Home/Sleep/Discover/Profile, floating pill), Peacock (icon-only, 5), Yelp (Search/Me/Collections/More). **Create-as-centered-FAB-in-the-bar** (Vinted's `+ Sell`, Beli's center action, plus Run-1 Jobber/Vimeo/Imgur/Locals/Craft/Clover/Canva/Docusign/Pillow/Trip.com center `+`) is a *distinct, very common* device: the create action sits visually apart (centered, often a filled circle/raised pill) inside an otherwise-flat tab bar — it is in the bar but not a peer destination.
- **Drawer / hamburger nav (re-confirm)** — Spotify (account drawer: Add account / Premium / What's new / Listening stats / Recents / Updates / Settings), X (Video/Chat/Communities/Bookmarks/Lists/Spaces/Creator Studio + Settings/Help — ~10), Rocket Money (Refer/Chat/Profile/Share/Budget/Categories/Linked Accounts/Notifications/Premium/Help — ~10+), Turo / Uber / Lyft / Nextdoor / Monese / Upwork (6–10-item account drawers over a scrim). All slide-in + scrim, rows ≥44px.
- **Filter / sort bottom sheet (re-confirm + count-on-primary)** — TheFork (`APPLY`), Beli (`Clear all` / `Apply (3)`), Instagram (`Clear` / `View 26 products`), Macy's (`Clear All 2` / `View 347 Items`), Booking.com (`Show 50 results`), AllTrails (`Clear` / `Show 7 trails`), Under Armour (`CLEAR ALL` / `SHOW 829 RESULTS`), DICK's (`SHOW 6139 ITEMS`), foodpanda (`Apply`), Zomato (left facet-nav rail *inside* the sheet + `Clear All` / `Apply`), Binance (`Reset` / `Confirm`), Tabby/Refine (`Reset` / `Show`), Ground News (`Clear` / `Show 4 result(s)`), Character AI (`Close`), Faire (`Apply`). The **result-count on the filled primary** is near-universal; **`Clear/Reset` (ghost, left) + `Apply/Show (N)` (filled, right)** is the dominant footer; an active-filter count badge on the trigger/title (`Sort & Filter (2)`, `Clear All 2`) recurs.
- **Bottom-sheet modal sizing (sharpens P-MO-09)** — partial: Revolut Business reason picker, Skip Menu Categories (+`Cancel`), Apple Music / Spotify / Notion add-to / share sheets, Cash App amount, Robinhood order, Calendar quick-add. Near-full-height: Tinder "Add birth place" (search), Uber Eats / eBay item detail, foodpanda Cuisines (search + checklist). Two-snap (peek→expanded): Airbnb date picker. Every one: drag handle + scrim + visible dismiss (X/Cancel/swipe). **No content-height threshold was uniform** — the split is content-driven (bounded → partial; search/long-list → tall), exactly as P-MO-09 states; snap points appeared only where two heights helped (Airbnb), confirming they're optional.
- **Detail → sticky bottom bar (re-confirm)** — Alan (`€41,99 ⓘ / €37,79 | Add to cart`), Tesla (price + `Select Option` + stepper → `Add To Cart`), Uber Eats (`Add 1 to cart · $2.89`), Skip (`Add To Cart $8.99`), SHEIN (`33% OFF! Add to Cart`), Shop (`Add to bag` + secondary `Buy now`), Blue Bottle (`Add $17.50` + `Bag`), Wish (`$1.50` + `Add to cart` with a *bottom tab bar above it*). The single filled primary + context-left holds; Shop stacks a *secondary* `Buy now` below the filled `Add to bag` (still one filled, F16).
- **Tablet / web-responsive band (NEW receipts — the previously-thin area)** — responsive *web* products that actually render the intermediate band: Linear, Stripe, Vercel, Notion, Retool, Mixpanel, Height, Coinbase, Asana, Figma, Dropbox, GitHub. Read for: **persistent left sidebar that condenses (icon-only / collapsible) before it ever becomes a hamburger** (Linear, Notion, Asana, Figma all collapse the rail to an icon rail at the medium band rather than dropping to a hamburger); **right detail/inspector pane that merges into the content column or moves below at the medium band before disappearing on phone** (Asana's right task pane, Coinbase's side panel); **data tables that drop secondary columns / horizontal-scroll at medium before row-stacking on phone** (Stripe, Coinbase, Height); **content grids stepping 4→3→2 rather than snapping** (Vercel projects, Figma files, Dropbox). None jumped N-col→1-col; the rail-condense-then-hamburger and table-scroll-then-stack sequences are real, observed transforms — the first concrete corroboration of P-MO-12 from shipped product rather than principle.

### Rule-application reading (Run 2)

| Rule | What Run-2 screens do | Verdict |
|---|---|---|
| P-MO-06 bottom-tab (count + overflow) | Etsy keeps create *out* of the bar entirely; Vinted/Beli put create as a **centered, visually-apart action inside** the bar; Run-1 + Run-2 together = ~15 apps with a center `+`/separated create | **VALIDATE + sharpen**: "create sits apart" has two equally-common realizations — (a) *outside* the tab bar (Glass floating `+`, Etsy omits it) and (b) a *centered, raised/filled* slot *inside* an otherwise-flat bar (Vinted `+ Sell`, Beli). Both keep it a non-peer. Worth naming the centered-FAB-in-bar variant explicitly so it isn't read as "a 5th tab." |
| P-MO-06 drawer (>5/deep) | Spotify/X/Rocket Money/Turo/Uber/Lyft/Nextdoor/Monese/Upwork — 6–10+ item account/settings drawers, slide-in + scrim, ≥44px rows | **VALIDATE**: deep/many → drawer holds across a fresh set; no app crammed 8+ destinations into a tab bar. |
| P-MO-08 filter sheet + count-on-primary | 15 fresh filter sheets, all sheets (zero rails — Zomato's facet-nav rail is *inside* the sheet, not a desktop rail); `Clear/Reset` + `Apply/Show (N)` footer dominant; result count on primary near-universal; active-count badge on trigger/title recurs | **VALIDATE + tighten Check**: the count-bearing primary and the Clear-left/Apply-right footer are consistent enough to state as the canonical anatomy (already in P-MO-08); add that the *trigger/title* often carries an active-filter count badge (`Sort & Filter (2)`). |
| P-MO-09 sheet sizing | partial for bounded pickers, near-full-height for search/long lists, two-snap only where it helps (Airbnb); every sheet handle+scrim+dismiss | **VALIDATE**: the contextual partial-vs-tall split and "snap points optional" both hold against a fresh set; no fixed height threshold — content drives it, as written. |
| P-MO-10 sticky primary | one filled verb + context-left across Alan/Tesla/Uber Eats/Skip/SHEIN/Shop/Blue Bottle/Wish; Shop's secondary `Buy now` stays subordinate | **VALIDATE**: F16-on-mobile (exactly one filled) and context-left hold; Wish even keeps the sticky buy-bar *above* a bottom tab bar (P-MO-11 chrome-coexistence corroborated). |
| P-MO-12 tablet band | web products condense the rail to icon-only before hamburger, merge/relocate the inspector pane at medium, scroll/shed table columns before row-stacking, step grids 4→3→2 | **VALIDATE (now with receipts)**: the previously principle-only P-MO-12 is corroborated by shipped responsive web — the intermediate transforms (rail-condense→hamburger, table-scroll→stack, grid-step, pane-merge) are real and observed. The honesty caveat in the prior section can be softened from "calibrated from principle" to "corroborated by responsive-web reference study." |
| P-MO-11 chrome over states | Wish keeps tab bar + sticky buy-bar together; filter sheets render their sticky Apply over scrolling facet lists | **VALIDATE**: sticky chrome coexists with the scrolling/!populated body. |

### Over-strict / wrong-rule catches (Run 2)
- **None.** A deliberate second pass over a fresh, non-overlapping screen set produced **zero TOO-STRICT findings and zero new-rule needs** — every P-MO rule was either re-validated or had a `Check:`-level detail sharpened. Per the convergence contract, this is a *converged* archetype: the only proposals are tightening wording (centered-FAB-in-bar variant; active-count badge on filter trigger; softening the P-MO-12 honesty caveat now that web receipts exist), not behavior changes.

### Net for patterns/mobile.md (Run 2)
- **VALIDATE all P-MO-01 … P-MO-12** against a fresh set.
- **Sharpen P-MO-06**: name the two realizations of "create sits apart" — outside the bar (floating/omitted) OR a centered/raised slot inside an otherwise-flat bar — so the centered `+` isn't mistaken for a peer tab.
- **Sharpen P-MO-08**: note the active-filter count badge on the trigger/title (`Sort & Filter (2)`) alongside the count on the Apply primary.
- **Soften the tablet-band honesty caveat**: P-MO-12 now has responsive-web receipts (rail-condense→hamburger, table-scroll→stack, grid-step, pane-merge), so it's no longer principle-only.
- **No numbers changed, no rules added, no rules loosened** — archetype reads as converged.
