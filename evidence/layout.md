# Evidence — Layout (composition grammar) (P-LY calibration)

Method: studied **12 real, public, shipped web screens across four distinct archetypes** — analytics **dashboard**, **marketing** landing, record **detail**, and **settings** — read by **(a) craft to learn** and **(b) how the composition grammar is applied in reality**: region decomposition (header / nav / canvas / rail / footer), column split, content measure/width, asymmetry vs symmetry, whitespace rhythm, and where real products diverge from a naive 12-column grid. Each observation is classified **CONFIRM** (the rule holds as written), **CALIBRATE** (a number/boundary needed fixing), or **TOO-STRICT** (the rule overfit and was loosened). Reading reality is the strongest calibration — it catches rules that are wrong, not just rules that are imprecise. Captured 2026-05-29.

Screens — **Dashboard:** Mixpanel (left grouped nav + pinned upgrade card; KPI-tile row over a segment table), Base44 (left nav + 2-up card grid: line chart wide, Page-Traffic/Country pair below), Linktree (left nav + 2-up map/cities cards + full-width device bar), Vimeo (left report nav + left-flush stat blocks beside a full-bleed map). **Marketing:** Relevance AI (top bar; **left-flush** narrow text hero, product-conversation cards below — not a 50/50 split), Otter.ai (top bar; classic **~7/5** text-left / media-right hero + trust-logo strip), Ferndesk (top bar; **~45/55** text-left / product-screenshot-right hero). **Detail:** Jobber (left app nav; main content split Location 4 / Overview-stack 8, success banner on top), Linear (left nav + main description column + **right properties rail ~8/4** + milestones), TheyDo (left nav + list + right inspector). **Settings:** Squarespace (left section nav + **centered ~640px form** nested in a wider content region), Lindy (left section nav, grouped, + centered form card), Linktree (left app nav + centered narrow account form). 2026-05-29.

## Rule-application table (rule × screens × verdict)

| Rule | What real screens showed | Verdict |
|---|---|---|
| **P-LY-01** region set from content shape | All six canonical splits appear in the wild: single column (Squarespace settings form body), main+right-rail (Linear 8/4), main+left-rail nav (every product app), 6/6-ish (none pure — see CALIBRATE), 7/5 asymmetric (Otter), 3-col (none in this set — rare, as the rule says) | **CONFIRM** — splits are content-shape-driven; the six cover the field. |
| **P-LY-02** left=nav, right=support | Universal: Mixpanel/Base44/Linktree/Linear/Jobber/Squarespace/Lindy all put nav LEFT; Linear/TheyDo put properties/inspector RIGHT. Zero counterexamples. | **CONFIRM** — strong, no exceptions found. |
| **P-LY-03** left sidebar for deep-nav product apps | 8/8 product surfaces used a persistent left sidebar; all three marketing pages used a top bar. Pinned account/upgrade card at rail bottom in Mixpanel, Base44, Linktree. | **CONFIRM** + adds the pinned-bottom-card default. |
| **P-LY-04** cap content to a measure inside a wide shell | Squarespace + Lindy settings nest a **~640px centered form** inside a much wider content region; Relevance/Otter hero text columns are ~480–560px inside full-width pages. | **CONFIRM** — measure-nesting is real and pervasive; the form is narrower than its region by design. |
| **P-LY-05** ≤3 measures on one axis (the spine) | Real product apps hold one app measure + a nested prose/form measure on a shared left edge. Marketing pages keep hero text and lower sections on one centered/left axis. | **CONFIRM** — the spine is what separates designed from assembled. |
| **P-LY-06** whitespace as hierarchy | Linear/Jobber group heading+body tightly, widen between blocks; settings pages separate field-groups with clear gaps. | **CONFIRM.** |
| **P-LY-07** asymmetry=priority, symmetry=equality | Marketing heroes are **all asymmetric** (Otter 7/5, Ferndesk 45/55, Relevance left-flush) — none is a literal 50/50. Dashboards use peer 2-up pairs (legit symmetry for equals). | **CALIBRATE** — see below; "symmetric split hero" is rarer than expected, asymmetric is the real default. |
| **P-LY-08** section rhythm ≠ inside-card rhythm | Marketing pages run large between-section gaps with tight inside-card stacks; dashboards run tight section gaps with even tighter card internals. | **CONFIRM.** |
| **P-LY-09** auto-fit unknown / fixed N-across | Dashboards lean on **2-up card pairs and auto-fitting tile rows**, NOT a rigid 12-col grid everywhere; KPI hero rows are fixed-count (Mixpanel). | **CALIBRATE** — the grid is looser in practice; 2-up pairs + auto-fit dominate. |
| **P-LY-10** reflow declared, no silent hide | (Desktop captures — reflow not directly observable; deferred to `patterns/mobile.md` evidence.) | **N/A here** — defers to mobile evidence. |
| **P-LY-11** pin only short supporting rails | Linear's right properties rail and milestones are short and effectively reference; main columns scroll. | **CONFIRM.** |

## Craft to copy
- **Pinned account/upgrade/trial card at the bottom of the left rail** (Mixpanel "Upgrade Plan", Base44 prompt card, Linktree "10+ new design tools") — a real, repeated dashboard-shell pattern; fed P-LY-03's default.
- **The hero text column is narrow even on a full-width page** (Relevance ~480px, Otter ~520px) — the F5 measure nested inside a full-bleed hero, not stretched. Fed P-LY-04.
- **Settings form is deliberately narrower than its content region** (Squarespace ~640px form in a ~900px region) — the clearest real proof that "let the container be wider than the text" is a feature, not a layout bug. Fed P-LY-04.
- **Dashboards compose from 2-up card pairs**, not a uniform 12-col grid — a line chart spans wide, then a Page-Traffic / Country pair sits 6/6 below (Base44, Linktree). The grid is a budget, not a straitjacket. Fed P-LY-09.
- **Marketing heroes commit to an axis** — every one was asymmetric or left-flush; none played it safe with a literal centered 50/50. The asymmetry *is* the priority signal. Fed P-LY-07.
- **Detail page = main reading column + right properties rail at ~8/4** (Linear) — the canonical record layout, distinct from the dashboard's table-led shell. Fed P-LY-01/02/11.

## Over-strict / wrong-rule catches (the strongest calibrations)
- **No marketing hero was a literal symmetric 50/50** — all three committed to an asymmetric or left-flush axis (Otter 7/5, Ferndesk 45/55, Relevance left-flush text-only). This **CALIBRATED P-LY-07**: the rule now states asymmetry is the *default* for a leading-vs-supporting hero and symmetry is reserved for genuine equals / display moments — rather than presenting 6/6 and 7/5 as co-equal first choices. The naive "split hero" (AP7) is exactly the safe move real products avoid.
- **Real dashboards don't lay out on a strict 12-column grid** — they compose from 2-up card pairs and auto-fitting tile rows that reshape with the container. This **CALIBRATED P-LY-09**: auto-fit is framed as the workhorse for equivalent/unknown sets, with fixed columns reserved for a deliberate "N across" — not "12-col grid is the default and auto-fit is the exception." A rule mandating rigid 12-col placement everywhere would have overfit.
- **The settings form being narrower than its region looked "wrong" against a naive full-width instinct** but is universal in shipped settings pages — this **CONFIRMED and sharpened P-LY-04** (measure nests *inside* region; the region is allowed to be wider than the text), preventing a future "stretch the form to fill the region" mistake.
- **Left-rail-for-nav held with zero counterexamples** across 8 product apps — **CONFIRMED P-LY-02/03** as a near-hard convention, not a soft preference.

## Calibrations applied to patterns/layout.md
1. **Six canonical region splits, chosen by content shape** — confirmed all present in the wild (P-LY-01).
2. **Left = nav, right = support; left sidebar for deep-nav product apps** — confirmed with zero counterexamples; added the pinned-bottom-card default (P-LY-02/03).
3. **Cap content to a measure nested inside a wider region** — confirmed by settings forms and hero text columns narrower than their containers (P-LY-04).
4. **Asymmetry = priority is the default hero move; symmetry only for genuine equals** — calibrated after every studied hero committed to an axis (P-LY-07).
5. **Auto-fit + 2-up pairs are the real grid workhorse; fixed columns for deliberate N-across only** — calibrated away from a rigid-12-col default (P-LY-09).
6. **≤3 measures on one axis (the spine)** + **whitespace as hierarchy** + **two distinct rhythm scales** — confirmed (P-LY-05/06/08).
7. **Reflow + mobile collapse** defer to `patterns/mobile.md`; **overlay-vs-page** defers to `patterns/containers.md` — kept this file to the composition skeleton only (P-LY-10).

---

## Run 2 — live-UI calibration pass (2026-05-29)

Method: a second **reference study of real, public, shipped products**, read this time purely for *composition grammar* — region split, content max-width/measure, rail width, section rhythm, sticky placement, and grid reflow. Corroboration came from (a) a screen library of real shipped web product captures across four archetypes and (b) rendered live homepages + primary design-system shell docs, cross-checked against each other. Same scheme — **CONFIRM / CALIBRATE / TOO-STRICT** — and the explicit discipline was *refine-don't-add*: no new rule unless battle-tested.

Screens / pages read this pass —
**Dashboard (left-shell + KPI + charts):** Mixpanel (Main Dashboard: KPI strip over a 2-up funnel/WAU pair, then full-width chart), Amplitude (left nav + KPI tiles + line chart + right realtime gauge rail + template card row), Base44/FlowFin (left nav + KPI row + 2-up Country/OS cards + a right "Customize dashboard" inspector), Squarespace Analytics (left nav + 4-up KPI strip + full-width line + 3-up source/device/browser row), Ferndesk Reporting (left nav + 3-up KPI cards + chart), ManyChat, Whop, Lovable/Nexus (dark dashboard: left nav + 4-up KPI + 2-up chart pair).
**Marketing (hero composition):** centered single-column — Chatbase (centered text + product card right? — text-left actually), **Vercel** (centered headline/CTA), **Arcade** (centered headline + URL field on gradient), **Craft** (centered headline + CTA + app screenshot below), **Shopify** (centered animated headline), **Claude** (split auth/marketing); asymmetric text-left/media-right — **Otter.ai** (~7/5 + trust strip), **Deel** (text-left / video-right), **Airwallex** (text-left / device-render right), **Hers** (text + phone-render, dark forest panel), **Pipedrive** (text-left / product-collage right), **Relevance AI** (left-flush narrow text, conversation cards below), **Fireflies** (left text / dark product panel right).
**Detail (main + right rail):** **Linear** (main description column + right Properties/Labels/Project rail, ~8/4), **Plane** (near-identical main + right Properties rail), **Attio** (left nav + activity main + right "Details" record rail), **Lightfield** (left nav + record table + right Account-details rail), **Salesforce** (left icon-nav + 3-region: About-left / Activity-center / Slack-right), Rox (modal detail + right notes), Wrangle (left nav + transcript main + right Details rail).
**Settings (left section-nav + form):** **Claude** (left section list + form, fields capped ~640px in a wider region), **Superlist** (left nav + centered card form + decorative image rail), **Perplexity** (left nav + centered ~640px form column), **Squarespace** (left section nav + form), **Cal.com** (left grouped section nav + form cards), **Linktree** (left app nav + centered narrow account form), Google Drive, X (3-pane: nav / section list / detail). Stamped 2026-05-29.

### Rule-application table (this pass)

| Rule | What real screens / specs showed this pass | Verdict |
|---|---|---|
| **P-LY-01** region set from content shape | All six splits re-appear: single-column (settings form bodies, centered marketing heroes), main+right-rail (Linear/Plane/Attio/Wrangle 8/4), main+left-rail nav (every product app), 7/5 asymmetric (Otter/Airwallex/Deel), 3-col (Salesforce About/Activity/Slack; X nav/list/detail). | **CONFIRM** (re-confirms Run 1). |
| **P-LY-02** left=nav, right=support; rail width | Left-nav universal; right rail = Properties/Details/realtime/inspector in Linear/Plane/Attio/Lightfield/Amplitude/Base44 — never swapped. **One calibration: the width number.** Shipped *nav* rails cluster **~240–280px** (shadcn 240/56-collapsed, Ant 200+8n, Carbon 256); the rule's "~320px default" describes a *content/properties* rail, not nav. | **CALIBRATE** (rail width by role) — see below. |
| **P-LY-03** left sidebar for deep-nav apps; top bar for marketing | ~14/14 product apps left-sidebar; every marketing hero top-bar. Pinned bottom card recurs (Mixpanel "Upgrade Plan", Base44 prompt card, Linktree "10+ new design tools", Attio "Add billing", Wrangle trial card). The **64px collapsed icon-rail** is the documented standard companion (shadcn/Ant/Carbon; Salesforce ships one live). | **CONFIRM** + adds 64px collapsed-rail default. |
| **P-LY-04** cap content to a measure nested in a wider region | Claude/Perplexity/Squarespace settings forms sit at **~600–720px centered inside a much wider region**; marketing hero text columns stay narrow (~480–560px) on full-width pages. Stripe marketing container ~1080px with nested narrower title measure. | **CONFIRM** — sharpens the form/prose nested band to **~600–720px**. |
| **P-LY-05** ≤3 measures on one axis (spine) | Settings hold one app measure + a nested form measure on a shared left edge; marketing holds container + nested narrower text on one centered axis. | **CONFIRM.** |
| **P-LY-06** whitespace as hierarchy | Detail pages group heading+body tight, widen between blocks; marketing runs ~80–120px between sections. | **CONFIRM.** |
| **P-LY-07** asymmetry=priority, symmetry=equality | **The catch of this pass.** The hero set splits ~evenly: **centered single-column** (Vercel, Arcade, Craft, Shopify) AND **asymmetric text/media** (Otter, Deel, Airwallex, Hers, Pipedrive, Relevance, Fireflies) are *both* shipped by best-in-class products. Run 1 (3 heroes) concluded asymmetric is *the* default and framed centered as the AP20 "safe, never-commits" move. A centered hero with one sharp value-prop + CTA + logo strip is a *committed* focus device, not an evasion. | **CALIBRATE** — make asymmetric and centered-single-column **co-equal committed defaults**; narrow the AP20 caution. See below. |
| **P-LY-08** section rhythm ≠ inside-card rhythm | Dashboards: tight section gaps over tighter card internals; marketing: large section gaps over tight card stacks. Held everywhere. | **CONFIRM.** |
| **P-LY-09** auto-fit unknown / fixed N-across | Re-confirmed strongly: dashboards compose from **KPI strips (fixed 3–4-up) + 2-up card pairs + auto-fitting tile rows** (Squarespace 4-up KPI then 3-up source row; Base44/Mixpanel 2-up pairs; Amplitude template card row), NOT a rigid 12-col everywhere. Practitioner consensus = hybrid (12-col frame for deliberate placement, `minmax(~280px,1fr)` auto-fit for uniform collections). | **CONFIRM.** |
| **P-LY-10** reflow declared, no silent hide | Live-desktop reads can't prove reflow; the **64px icon-rail** observed as the documented intermediate step before drawer reinforces the ladder. Defers to `patterns/mobile.md`. | **N/A here** (corroborates the ladder). |
| **P-LY-11** pin only short supporting rails | Linear/Plane/Attio right Properties rails are short reference surfaces; Amplitude's realtime gauge rail is short; main columns scroll. | **CONFIRM.** |

### Over-strict / wrong-rule catches (the strongest calibrations this pass)
- **P-LY-07 — centered single-column heroes are NOT the AP20 safe-regression tell.** Run 1 read three asymmetric heroes and concluded asymmetric is *the* default, framing centered as "never commits." This pass, an even split of best-in-class pages ship **centered single-column heroes** (Vercel, Arcade, Craft, Shopify) as a deliberate one-message focus device — headline + subhead + single CTA + logo strip below. That is a *committed* axis, not an evasion. The real tell is **failure to commit to ANY focal moment** — centered *everything* top-to-bottom with no anchor (already covered by AP6). Calibration: present **asymmetric (one side leads + media) and centered-single-column (one dominant message) as co-equal first choices, chosen by content shape** — message+demo ⇒ asymmetric; one sharp message ⇒ centered — and narrow the AP20 caution to "centered-everything with no focal commitment," not "any centered hero."
- **P-LY-02 — nav-rail width "~320px" is wider than reality.** Shipped *navigation* rails cluster **240–280px** (shadcn 240px expanded / 56px collapsed, Ant `200+8n`, Carbon 256px). 320px belongs to a *content/properties* rail (a card + padding). The rule conflates the two roles under one number. Calibration: **nav rail 240–280px; supporting/content right rail ~320px (280 dense); >360px = a second column** — keeps the upper bound, splits the default by rail role.

### Craft to copy (this pass)
- **Collapsed 64px icon-rail is the standard companion to a 240–280px expanded nav** (shadcn/Ant/Carbon; Salesforce live) — the documented step before the rail becomes a drawer; reinforces the P-LY-03 / P-LY-10 "icon-rail → drawer" ladder.
- **Dashboard = KPI strip (fixed 3–4-up) → 2-up card pairs → auto-fitting tile row**, not a uniform 12-col grid (Squarespace, Base44, Mixpanel, Amplitude) — the grid is a budget, not a straitjacket. Re-confirms P-LY-09.
- **Right-side dashboard inspector** (Base44 "Customize dashboard", Amplitude realtime rail) — a short supporting right rail on a dashboard, same role as a detail-page properties rail (P-LY-02/11).
- **Settings form ~600–720px centered inside a wider region** (Claude, Perplexity, Squarespace) — the clearest live proof "let the container be wider than the text" is a feature (P-LY-04).
- **Pinned trial/upgrade/billing card at left-rail bottom** recurs across products (Mixpanel, Base44, Linktree, Attio, Wrangle) — re-confirms P-LY-03's default.

### Calibrations proposed from this pass (gated by the user)
1. **P-LY-07** — reframe asymmetric and centered-single-column as **co-equal committed hero defaults chosen by content shape**; narrow the AP20 caution to "centered-everything, no focal commitment." (CALIBRATE)
2. **P-LY-02** — split the rail-width default by role: **nav rail 240–280px**, support/content right rail **~320px (280 dense)**, >360px = second column. (CALIBRATE)
3. **P-LY-03** — add the **64px collapsed icon-rail** as the documented default companion to the expanded nav rail (CONFIRM + small addition).
4. **P-LY-04** — record the observed **~600–720px** settings/prose nested-form band (well inside F5) as the concrete number (CONFIRM, sharpen).

Gate: contrast re-checked on the layout-mockup palette via `scripts/contrast.mjs` — `#1f2937:#fafafa` 14.06:1, `#374151:#fff` 10.31:1, `#6b7280:#fff` 4.83:1, `#ffffff:#0b6b3a` 6.61:1 — all clear AA. No new rule added (refine-don't-add held; the two CALIBRATEs are number/branch refinements, not additions).
